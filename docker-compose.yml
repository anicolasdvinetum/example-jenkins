version: '3.9'

services:
  go-service:
    build: ./service1
    container_name: service1
    ports:
      - "8080:8080"

  python-service:
    build: ./service2
    container_name: service2
    ports:
      - "5000:5000"